# Config locale de la machine / Claude Code
.claude/
*.credentials.json
.credentials.json

# Cles privees de signature de code : ne doivent JAMAIS entrer dans le depot
*.pfx
*.p12
*.key

# Divers
*.log
Thumbs.db
desktop.ini

# Journal de diagnostic (une ligne par relève, local à chaque poste)
ClaudeTrace.csv
ClaudeTrace.csv.1

# État local (position/taille de la fenêtre)
widget_pos.json

# Audio dérivé (embarqué en base64 dans le script)
nyan.wav
dj-sound.wav
default-sound.wav
